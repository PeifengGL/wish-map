import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  popupModalProgressBarContainer: {
    width: '100%',
    height: 5,
    backgroundColor: '#ECECEC',
    borderRadius: 100,
    overflow: 'hidden',
  },
  popupModalProgressBar: {
    height: '100%',
    backgroundColor: '#0057B8',
  },
});

export default Styles;
